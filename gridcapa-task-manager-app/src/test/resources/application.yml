task-server:
  process:
    tag: CSE_D2CC
    timezone: CET
    inputs:
      - CGM
      - CRAC
    outputs:
      - CNE

minio-adapter:
  bucket: gridcapa
  base-path: /cse/d2cc
  url: http://minio.url
  access-key: gridcapa
  secret-key: gridcapa